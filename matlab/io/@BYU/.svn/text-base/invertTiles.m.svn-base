function [byu]	= invertTiles(byu)
%
% function [byu]	= invertTiles(byu)
%
% Flips the tiles (if they were clockwise, they now become ccw and so on)
%

byu.adjacency = byu.adjacency(:,end:-1:1);

return;
